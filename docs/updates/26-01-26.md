# Session Log - 26/01/26

## Rebranding Updates

Renamed key features and updated branding throughout the application:

**Navigation & Labels:**
- "Circles" → "Think Tanks" (EN) / "Tankesmedjor" (SV)
- "Learning" → "Micro-Courses" (EN) / "Mikrokurser" (SV)
- Updated Sidebar.jsx fallback labels

**Branding:**
- "Deburn" → "Human First AI" across all auth pages (Login, Register, ForgotPassword, ResetPassword)
- Updated all legal pages (PrivacyPolicy, TermsOfService, CookiePolicy)
- Updated localStorage keys: `deburn_auth_token` → `hfai_auth_token`, `deburn_coach_conversation` → `hfai_coach_conversation`
- Updated comments in Hub.jsx, hub.css, prototype.css

---

## Coach Page Updates

**Hero Tagline:**
- Changed from "Your personal leadership companion" to "Your leadership coach"
- Updated both EN and SV translations

**Chat Message Formatting:**
- Added `formatCoachResponse.jsx` utility for markdown-like formatting
- Converts `**bold**` → `<strong>`, `- bullets` → `<ul><li>`, line breaks → `<br>`
- Added `FormattedMessage` component for completed messages
- Added `StreamingMessage` component for progressive formatting during streaming
- Streaming now formats text in real-time (same approach as Claude/ChatGPT)

**Files Changed:**
- `src/pages/Coach.jsx` - Uses FormattedMessage and StreamingMessage components
- `src/utils/formatCoachResponse.jsx` - New formatting utility
- `src/locales/en/coach.json` - Updated tagline
- `src/locales/sv/coach.json` - Updated tagline

---

## Micro-Courses Page Updates

**Filtering:**
- Added category filter buttons: All, Featured, Leadership, Breath, Meditation
- Filter state persists while browsing
- Styled with pill-shaped buttons matching design system

**Duration Display:**
- Added duration badge to course cards showing `X min` from MongoDB `lengthMinutes` field

**Translations:**
- Updated hero title to "Micro-Courses" / "Mikrokurser"
- Added filter labels

**Files Changed:**
- `src/pages/Learning.jsx` - Added filtering and duration display
- `src/prototype.css` - Added `.learning-filters`, `.filter-btn`, `.learning-card-duration` styles
- `src/locales/en/learning.json` - Updated title, added filters
- `src/locales/sv/learning.json` - Updated title, added filters
