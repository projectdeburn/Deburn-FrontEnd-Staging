# Session Log - 20/01/26

## Session - Responsive UI & i18n Fixes

### Continue Button Fix (Checkin Page)

Fixed the continue button getting cut off when resizing the page vertically or on mobile:

- Updated `.checkin-screen` and `.checkin-container` to use `100dvh` (dynamic viewport height) for Safari compatibility
- Made `.checkin-container` use fixed height with `overflow: hidden`
- Made `.checkin-step.active` scrollable with `overflow-y: auto`
- Added `flex-shrink: 0` to `.checkin-nav` to prevent it from being pushed off-screen

---

### Mobile Bottom Navigation

Added mobile bottom navigation that appears when screen width is below 768px:

- Added `<nav className="mobile-nav">` component to `Layout.jsx` with all navigation items (Home, Check-in, Eve, Learn, Circles, Feedback, Admin)
- Admin tab only visible for admin users
- Updated CSS to properly handle Safari safe area insets

---

### Sidebar Active State Fix

Fixed the Circles tab incorrectly highlighting when on the Admin page:

- Added `end: true` prop to Circles and Dashboard nav items in both `Sidebar.jsx` and `Layout.jsx`
- This prevents partial path matching (`/circles` was matching `/circles/admin`)

---

### i18n Translation Fixes

#### Learning Page Localization

- Added `getLocalizedField()` helper to select `titleEn`/`titleSv` based on current language
- Updated category names to use `t()` function instead of hardcoded English
- Added missing category translations (featured, breath, burnout, wellbeing, other) to both `en/learning.json` and `sv/learning.json`

#### ArticleModal & AudioModal Localization

- Updated both modals to use localized content (title, textContent)
- AudioModal now fetches audio in the correct language (`/audio/en` or `/audio/sv`)
- Added `minRead` and `noModules` translations

#### CirclesAdmin Page Translations

- Created `en/circlesAdmin.json` with all Admin page translations
- Created `sv/circlesAdmin.json` with Swedish translations
- Registered `circlesAdmin` namespace in `i18n.js`

#### Sidebar Navigation Translations

- Added `admin` key to nav translations in both locales
- Translated "Feedback" to "Återkoppling" in Swedish

---

### Hub Route Disabled

Commented out the `/hub` route in `App.jsx` to hide it from users:

- Route is commented out but code is preserved
- Accessing `/hub` now redirects to dashboard via catch-all route
- Can be re-enabled by uncommenting the route

---

### Route Restructure - Login as Default

Changed routing so `/` redirects to login (or dashboard if authenticated):

- Added `RootRedirect` component that checks auth state and redirects appropriately
- Dashboard moved from `/` to `/dashboard`
- Updated `PublicRoute` to redirect authenticated users to `/dashboard`
- Updated catch-all route to redirect to `/login`

**Files updated with new `/dashboard` path:**
- `App.jsx` - New route structure with RootRedirect
- `Sidebar.jsx` - Dashboard nav link
- `Layout.jsx` - Mobile nav dashboard link
- `Login.jsx` - Post-login redirect
- `Checkin.jsx` - Back button and completion redirect
- `Profile.jsx` - Back button
- `Admin.jsx` - Non-admin redirect and back button
- `CirclesAdmin.jsx` - Non-admin redirect
- `Hub.jsx` - Return button

Legal pages (`PrivacyPolicy`, `TermsOfService`, `CookiePolicy`) keep `Link to="/"` - RootRedirect handles routing appropriately for both authenticated and unauthenticated users.

---

### Files Modified

- `src/App.jsx` - Route restructure, RootRedirect, hub disabled
- `src/prototype.css` - Responsive fixes for checkin page and mobile nav
- `src/components/layout/Layout.jsx` - Added mobile navigation
- `src/components/layout/Sidebar.jsx` - Added `end` prop for nav items
- `src/pages/Learning.jsx` - Localization support
- `src/components/learning/ArticleModal.jsx` - Localization support
- `src/components/learning/AudioModal.jsx` - Localization support
- `src/locales/en/learning.json` - Added categories and UI strings
- `src/locales/sv/learning.json` - Added categories and UI strings
- `src/locales/en/common.json` - Added admin nav key
- `src/locales/sv/common.json` - Added admin nav key, translated feedback
- `src/locales/en/circlesAdmin.json` - New file
- `src/locales/sv/circlesAdmin.json` - New file
- `src/utils/i18n.js` - Registered circlesAdmin namespace
